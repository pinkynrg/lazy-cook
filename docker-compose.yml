version: '3.8'

services:
  app:
    build: .
    container_name: lazy-cook
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /mnt/data/lazy-cook:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
